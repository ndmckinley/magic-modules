---
platform: linux

image_resource:
    type: docker-image
    source:
        repository: gcr.io/magic-modules/python
        tag: '1.0'

inputs:
    - name: magic-modules-experimental-prs

outputs:
    - name: comment

run:
    path: magic-modules-experimental-prs/.ci/magic-modules/welcome-contributor.sh
